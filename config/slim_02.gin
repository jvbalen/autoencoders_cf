# sparse block-wise SLIM a la Steck

experiment.Recommender = @LinearRecommender
# experiment.cap = 10000

LinearRecommender.save_weights = False
LinearRecommender.weights_fn = @block_slim

block_slim.target_density = 0.004
block_slim.row_nnz = 1000
block_slim.l2_reg = 1.0
block_slim.r_blanket = 0.5    
