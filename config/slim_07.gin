# sparse simple cholesky embeddings

experiment.Recommender = @EmbeddingRecommender
# experiment.cap = 10000

EmbeddingRecommender.save_embeddings = False
EmbeddingRecommender.embedding_fn = @simple_cholesky_embeddings

simple_cholesky_embeddings.l2_reg = 500.0
simple_cholesky_embeddings.row_nnz = 100
