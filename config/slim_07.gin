# svd embeddings

experiment.Recommender = @EmbeddingRecommender
# experiment.cap = 10000

EmbeddingRecommender.save_embeddings = False
EmbeddingRecommender.embedding_fn = @svd_embeddings

svd_embeddings.k = 1000
svd_embeddings.square = False
svd_embeddings.row_nnz = 100
