# cholesky-based SLIM (mostly sanity check)

experiment.Recommender = @LinearRecommender
# experiment.cap = 10000

LinearRecommender.save_weights = False
LinearRecommender.weights_fn = @cholesky_slim

cholesky_slim.l2_reg = 200.0
cholesky_slim.row_nnz = 120
