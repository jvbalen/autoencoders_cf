# 1-layer WAE with NLL loss, bias and dropout

build_model.Model = @WAE
build_model.n_layers = 1
build_model.slim_path = '/home/jvanbalen/experiments/wae/pruned_logistic.npz'

sparse_tensor_from_init.randomize=True

WAE.loss = "nll"
WAE.use_biases = True
WAE.normalize_inputs = False
WAE.shared_weights = True
WAE.keep_prob = 0.8
WAE.lam = 1e-3
WAE.lr = 3e-4

train.batch_size = 100
train.n_epochs = 10
