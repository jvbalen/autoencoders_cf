# head tail SLIM - 99 pct sparse

experiment.Recommender = @LinearRecommender
# experiment.cap = 10000

LinearRecommender.density = 0.01
LinearRecommender.reg = 200
LinearRecommender.head_tail = True
LinearRecommender.incremental = False
LinearRecommender.save_weights = False
